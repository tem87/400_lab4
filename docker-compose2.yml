services:

  # Building and testing environment
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: alpine:latest
    stdin_open: true
    tty: true
    command: ["/bin/sh"]
    volumes:
      - /Users/austinkim/ENSF400/toolbox
      - /var/run/docker.sock:/var/run/docker.sock
    network_mode: "host"
